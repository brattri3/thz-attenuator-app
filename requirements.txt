# Рантайм приложения. Ставится обычным `pip install -r requirements.txt`.
numpy>=1.20.0
matplotlib>=3.4.0

# --- интерфейс CW-приложения ----------------------------------------------
# Ставить ОТДЕЛЬНО, не вместе с остальным: полный PySide6 тянет QtWebEngine,
# Qt3D и Charts -- сотни мегабайт, из которых приложению не нужно ничего.
#
#     pip install PySide6-Essentials==6.11.2 pyqtgraph==0.14.0
#
# Сервисные окна (tools/service_gui.py) работают на tkinter из стандартной
# поставки Python и отдельной установки не требуют.
