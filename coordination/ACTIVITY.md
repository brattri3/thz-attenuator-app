# ACTIVITY — append-only project journal

Every role appends here on a significant change: a finding, a decision, a paradigm-level
observation, a completed piece of work worth other roles knowing about. **Append-only** — past
entries are never rewritten, even when later work supersedes them; if something is superseded,
say so in a *new* entry, don't edit the old one.

This file will grow large. That's fine — it's meant to be searched by date and tag, never read
in full. Once it's grown past a size where a fresh session shouldn't read it at cold start, say
so explicitly in `CLAUDE.md`'s index table (see that file's "what to read and when" section) so
sessions don't accidentally pay for reading the whole thing.

Format: one entry per change, dated, tagged with the acting role's `[<ID>]`. A loose shape that
works:

```
## [YYYY-MM-DD] [<ID>] short title
What happened / what was decided / what was found. Link to the relevant file+line rather than
pasting large excerpts. If this closes out detail that used to live in a role's
`roles/<ID>.md`, that's a good reason for an entry — the detail moves here, the role file keeps
only the live summary (see `roles/ROLE_ID.md` template for why that split matters).
```

Tag proposals about the coordination process itself with `[proposal]` (see `CHARTER.md §10`) so
the orchestrator can find them without reading every entry.

## [2026-08-27] [APP] Три правки интерфейса из четырёх сделаны, третья остановлена на П-2

Работа шла в песочнице `.worktrees/app` (ветка `role/app`), три коммита.

**Правка 1 — кнопки вверх у `QDoubleSpinBox`.** Гипотеза владельца про верхнюю границу не
подтвердилась: щелчок по верхней стрелке исправно шагал значение и до правки. Причина в
`theme.py` -- правило `QDoubleSpinBox { background/border/padding }` переводило виджет в разбор
таблицей стилей целиком, и стрелки вырождались в закорючку 4 px при кнопке 20 px. Задать
`::up-button` явно нельзя: без `image:` Qt не рисует стрелки вовсе (проверено снимком). Спинбокс
отдан нативному стилю -- стрелка стала 10 px при кнопке 16 px, и стиль сам красит её серым при
упоре в границу диапазона.

**Правка 2 — слайдеры к `θ₁` и `θ₂`**, цена деления 1°, синхронно с полем в обе стороны. Флаг
`_syncing` не даёт целочисленному слайдеру округлить введённые с клавиатуры 30.25° обратно до 30.

**Правка 3 — нормировка при повёрнутом источнике: ОСТАНОВЛЕНА.** Условие хэндоффа «опора при
фактическом ψ» оказалось уже выполненным в коде (`compute` зовёт `_apply` до `_reference_db`).
Осталась ровно развилка П-2: опора приколочена к оси прибора (θ₁ = θ₂ = 0), и при ψ = 45°
максимум уезжает на +23° и превышает 0 дБ на 1.64 дБ. Числа и цена каждого варианта -- в
`QUESTIONS.md`, подраздел «П-2 — уточнение от APP». Работа ждёт ответа владельца.

**Правка 4 — поиск паспорта рядом с программой**, ловушка PyInstaller закрыта: под сборкой
каталог берётся от `sys.executable`. Явно выбранный негодный файл отказывает громко и оставляет
прежний паспорт; посторонний JSON рядом лишь пропускается с пометкой. Источник паспорта виден в
строке состояния.

**Проверки.** Каждая правка проверена живым запуском окна и снимком, не импортом. На каждый
дефект заведена машинная проверка, и каждая проверена откатом -- краснеет на дефекте:
`cwapp.selftest` вырос с 12 до 17 (M13…M17: порядок поиска, поддельный `sys.frozen`, громкий
отказ явного файла, имя файла в строке состояния, образец последним рубежом), дымовой прогон --
до 70 проверок и 15 снимков. Первая редакция пиксельной проверки стрелок считала чернила вместе
с рамкой кнопки и давала одинаковые числа до и после правки -- переписана на ширину стрелки в
долях ширины кнопки.

⚠ **Ожидаемое число приёмки в `CLAUDE.md` устарело:** там `cwapp.selftest # 12/12`, фактически
17/17. Файл в зоне ORCH -- запись заведена в `HANDOFFS.md`.

Приёмка после каждой правки: cwapp 17/17, база 13/13, сервисное (модель OK, пределы OK,
калибровка 8/8, приложение 7/7, A7 пропущена штатно), CW-кривая 5/5, сверка ядра 9 файлов без
расхождений. Опорная точка CLI сходится: −4.77 дБ, азимут +0.023°, эллиптичность +0.630°.

## [2026-08-27] [APP] Владелец закрыл П-2: нормировать на максимум. Правка 3 разблокирована

Ответ владельца на опросник: опора -- **максимум кривой**, а не отсчёт при θ₁ = θ₂ = 0. Решение
записано в `QUESTIONS.md` (строка П-2 закрыта, следствия разобраны подразделом). Что оно тянет за
собой: 0 дБ уезжает вместе с ψ, прогоны с разным наклоном источника несравнимы по абсолютной
величине, показание окна расходится с `service_calc` при ψ ≠ 0, проверка M2 приёмки верна только
при ψ = 0 и требует пересмотра. Опорная точка сверки из `CLAUDE.md` не задета: там ψ = 0.

Прочие ответы: `git push` пока не делать (четыре коммита остаются в локальной ветке `role/app`);
песочницу `.worktrees/app` убрать **после мержа**, не раньше; из предложенных направлений ничего
не начинать, а сохранить их в дорожную карту.

Дорожная карта дополнена: заморозка эталона `A7` в `tests/golden/` внесена в 0.2.0, переключатель
опоры «максимум / ось прибора» -- к пункту 0.5.0 о выборе опоры нормировки. Сборка `.exe`, диалог
ошибки с логом и окно «О программе» там уже были.

## [2026-08-27] [APP] Дашборд слежения осмотрен: экран расходится с журналами, запись небезопасна

По просьбе владельца дашборд поднят на `localhost:8502` и осмотрен целиком **в режиме только
чтения** -- ни одна форма не отправлена, файлы и git не тронуты. Из пяти вкладок данные без
искажений показывает только Git Worktrees. Парсер написан под английские журналы: «открыт» и
«блокирующий» не распознаются, схема нашего `BOARD.md` не совпадает с ожидаемой, записи
`HANDOFFS.md` от 27.08 выводятся пустыми, `ACTIVITY.md` и `roles/*.md` не читаются вовсе. Ошибок
разбора дашборд не показывает -- расхождение молчаливое.

Запись опаснее чтения: `append_question` вставит новый вопрос внутрь таблицы с физическими
числами П-2, «Register New Role» пишет за пределы таблицы `BOARD.md`, подтверждений нет ни у
одного действия, а выключатель git-автокоммита недоступен -- боковая панель в этой сборке
Streamlit не рендерится, дефолт «включено».

Дашборд в зоне ORCH и идёт чистым апстримом -- правок здесь не делал. Полный разбор с привязками
к файлам и строкам передан записью «ОТ APP К ORCH» в `HANDOFFS.md`. Решение о репорте в апстрим --
за ORCH и владельцем.

## [2026-08-27] [ORCH] П-1 и П-3 закрыты, работа APP принята и влита, ревизия скилла

**Приёмка прогнана самостоятельно, не со слов.** На основной линии: `cwapp` 12/12, база 13/13,
сервисное (модель/пределы/калибровка/приложение) OK, CW-кривая 5/5, сверка ядра 9 файлов.
На ветке `role/app` в песочнице `.worktrees/app` (дерево чистое): `cwapp` **17/17**, остальные
три прогона те же, ядро 9 файлов. Опорная точка сверки интерфейса против CLI
(`--from-angle 40 --theta2 0 --freq 0.8`) даёт −4.77 дБ, азимут +0.023°, эллиптичность +0.630° —
совпадает с записанным в `CLAUDE.md`. После этого ветка влита мержем `--no-ff`; `git push`
не делался.

**Решения владельца, записаны в `QUESTIONS.md` строками:**

- **П-1 — поставка не onefile.** `COLLECT`, Qt-DLL отдельными файлами рядом с `.exe`: требование
  LGPL закрывается формой поставки, а не обещанием приложить исходники. Наружу уходит zip-архив,
  SHA-256 (§6 п.4) считается на архив — требование контрольной суммы не снимается. Исполнение в
  `build/gui.spec` — задание APP, файл в их зоне.
- **П-3 — тег `v0.1.0` отложен** до правки 3: нормировка на максимум меняет выдаваемые числа, и
  релиз до неё устарел бы в тот же день. Версия в `cwapp/__init__.py` уже `0.1.0`, порядок §6
  продолжится с шага 2 (CHANGELOG).

**Ревизия инструментов.** Апстрим `DimaMuhannad/multi-agent-coordination-skill` стоит на том же
`2e37500` (наш `#5` смержен, поверх ничего нет) — обновлять нечего, локальных правок по-прежнему
нет. ⚠ Клон на Windows падает на чекауте из-за 22 `.pyc` и `.coverage` с длинными именами; для
ревизии хватило `git log` по неудавшемуся чекауту — файлы качать не требуется.

**Дашборд.** Разбор APP принят: решение владельца — репорт в апстрим (как с хуком, без форка) и
пометка «не использовать на запись». Пометка проставлена в `CLAUDE.md`, `BOARD.md` и `CHANGELOG.md`
с двумя причинами: молчаливое искажение показаний на русских журналах и `append_question`,
вставляющий строку внутрь таблицы физических чисел по П-2. Черновик issue лежит в
`coordination/upstream/issue-dashboard.md` — публикация ждёт отдельного разрешения владельца.

**Что осталось за владельцем:** разрешение на публикацию issue и на `git push` (обе локальные
линии — основная с мержем и `role/app` — не отправлялись).

## [2026-08-27] [ORCH] Пуш выполнен с разрешения владельца; публикация issue упирается в инструмент

Владелец разрешил публикацию репорта по дашборду и `git push`. Что вышло:

- **Пуш сделан, fast-forward, без форса.** `origin/main` 83a5342 → 121b3ea, отдельно отправлена
  ветка `role/app` (её коммиты и так вошли в main мержем — ветка опубликована как след работы).
  Дифф перед отправкой просмотрен: 17 файлов, данных экземпляра прибора нет, совпадения по слову
  `passports/` — только пути и текст проверок M13, не содержимое паспортов.
- **Issue не опубликован:** `gh` в системе не установлен, токена в окружении нет. Доставать
  учётные данные из Git Credential Manager (которыми прошёл пуш) ради вызова API не стал.
  Черновик готов и лежит в `coordination/upstream/issue-dashboard.md`; публикуется либо после
  `winget install GitHub.cli`, либо вручную через веб-форму апстрима.

⚠ Локальный `main` в основном каталоге остался на 83a5342: он занят тем checkout, из песочницы
его не продвинуть. Удалённый `main` уже новый — расхождение снимается одним `git pull` там.

## [2026-08-27] [ORCH] Репорт по дашборду опубликован в апстриме — issue #6

Владелец отправил текст через веб-форму: `gh` в системе так и не появился (проверено трижды —
ни в `Program Files\GitHub CLI`, ни в `WinGet\Links`, ни среди пакетов winget), а установить
его из моей оболочки нельзя: winget требует интерактивного согласия с условиями.
Опубликовано как
[issue #6](https://github.com/DimaMuhannad/multi-agent-coordination-skill/issues/6), состояние
`open`. Черновик оставлен в `coordination/upstream/` как запись о том, что именно отправлено.

Ссылка на issue проставлена в `CLAUDE.md`, `BOARD.md`, `CHANGELOG.md` и `roles/ORCH.md` — там,
где стоит пометка «дашбордом не пользоваться на запись»: пометка снимается ответом апстрима, а не
временем.

**Заход ORCH на этом закрыт.** Открытыми у ORCH остаются только ожидание правки 3 (после неё —
CHANGELOG → тег `v0.1.0` → сборка папкой → zip + SHA-256) и ответ апстрима по issue #6.

## [2026-08-27] [ORCH] Дашборд остановлен

Экземпляр, поднятый APP для осмотра (`localhost:8502`, PID 7248), снят по указанию владельца —
порт свободен. В записи APP он числился запущенным; теперь нет.

Останов **процесса**, не отключение инструмента: `streamlit run coordination/tools/dashboard/…`
по-прежнему поднимет его снова. Запрет на использование дашборда на запись держится пометками в
`CLAUDE.md` и `BOARD.md` и ждёт ответа апстрима по issue #6. Если понадобится закрыть путь
совсем — это локальная правка поверх чистого апстрима (снять команду из `CLAUDE.md`, закрыть
каталог), и её придётся повторять при каждом обновлении скилла; отдельного решения владельца на
это не было.

⚠ На машине работали ещё два процесса `streamlit run coordination/tools/dashboard/dashboard.py`
из venv другого проекта (`tydex-products-toolkits`). Они не наши — не тронуты.


## [2026-08-28] [APP] Правка 3, поставка папкой и проверка обновлений -- путь к `v0.1.0` пройден

Владелец выбрал сценарий «весь путь до релиза», номер `v0.1.0` без поднятия версии, проведение --
через ORCH. Три коммита в `role/app`.

**Правка 3 (П-2).** Опора -- максимум по поверхности T(θ₁, θ₂), а не по сечению: опора, зависящая
от закреплённого угла, прыгала бы при каждом движении второго ротатора. При ψ = 0 максимум лежит в
нуле шкал, поэтому прежние числа не сдвинулись ни на знак. M2 переписан -- он проверяет частный
случай ψ = 0; добавлены M18…M21. Первые редакции M19 и M20 были неверны (искали максимум
поверхности в сечении, сравнивали сечения при разных закреплённых углах) -- переписаны.

**Поставка (П-1).** `EXE + COLLECT`, своя точка входа `cwapp_entry.py` -- спека собирала
tkinter-линию, а продукт cwapp. `LICENSES.txt` рядом с `.exe`, а не в `_internal`. Исключён scipy:
тянулся транзитивно через pyqtgraph, 68 МБ из 215, и при полном расчёте не импортируется вовсе.
Ловушка PyInstaller проверена на собранной папке: паспорт, положенный рядом с `.exe`, подхвачен --
из исходников этот дефект не проявляется никак.

**Обновления.** `QtNetwork` из уже стоящего Essentials, разбор -- отдельным модулем без Qt и без
сети. Четвёртый исход «релизов ещё нет» добавлен к трём из плана: до первого тега GitHub отвечает
404. TLS в упакованном виде проверен консольным зондом -- PyInstaller регулярно теряет TLS-плагин,
и тогда любой запрос молча становится отказом; в сборке openssl и schannel на месте.

Приёмка: cwapp 27/27, база 13/13, сервисное OK, CW-кривая 5/5, ядро 9 файлов, дымовой прогон
зелёный с живым запросом, опорная точка CLI −4.77 дБ. CHANGELOG, тег, zip и SHA-256 переданы ORCH
записью в `HANDOFFS.md` -- эти файлы в его зоне.

## [2026-08-28] [APP] Заявка владельца на развитие проверки обновлений -- в план, без реализации

Владелец 28.08 попросил запланировать три надстройки над готовой проверкой обновлений:
автоматическую проверку без нажатия кнопки, подсветку кнопки при наличии новой версии и показ
ссылки на репозиторий с патчноутами, когда интернета нет. Записаны в дорожную карту как **0.2.1**
(`13_CW_APP_RELEASES.md` §3). Реализация не начиналась -- заявка на планирование.

Три вещи, которые придётся решить до реализации, отмечены прямо в плане:

- **молчаливый исходящий запрос с приборной машины -- решение владельца.** Автопроверка означает
  обращение наружу без ведома оператора; у спектрометра сеть бывает изолированной. Нужен явный
  выключатель и вопрос при первом запуске;
- **время последней проверки нельзя писать рядом с `.exe`** -- поставка может лежать в каталоге
  только для чтения; место для него -- пользовательский каталог настроек;
- **подсветка кнопки наступает на ту же ловушку, что и правка 1**: правило таблицы стилей на
  `QPushButton` отключит нативную отрисовку, и проверять придётся снимком живого окна.

Отдельно: автоматическая ПРОВЕРКА не отменяет правила «автообновления не будет никогда» -- речь о
чтении списка релизов, а не о самоизменении программы между измерениями одной серии. При
автопроверке диалогов быть не должно вовсе: единственный видимый след -- подсветка кнопки.

## [2026-08-28] [ORCH] Приёмка работы APP: принято, с одной оговоркой по оснастке

Четыре прогона, сверка ядра и дымовой прогон — своими руками, на объединённом состоянии после
мержа, а не на словах передачи:

| Прогон | Число | Ожидалось |
|---|---|---|
| `cwapp.selftest` | **27/27** | 27/27 (было 17/17) |
| `attenuator_app.selftest` | 13/13 | 13/13 |
| `service_selftest` | модель/пределы/калибровка/приложение OK | OK |
| `cw_curve --selftest` | 5/5 | 5/5 |
| `verify_core.py` | 9 файлов, расхождений нет | 9 файлов |
| `smoke_cwapp` (QT_QPA_PLATFORM=windows) | всё зелёное, 15 снимков | зелёное |

Опорная точка CLI (40°/0°/0.8 ТГц) — −4.77 дБ, азимут +0.023°, эллиптичность +0.630°: правка 3
при ψ = 0 чисел не сдвинула, как и заявлено. Ядро не тронуто.

**Расхождение с передачей одно.** Дымовой прогон, запущенный как написано в документации (без
переменной окружения, платформа `offscreen`), даёт 8 отказов по стрелкам спинбокса — под
offscreen нативный стиль их не рисует. С `windows` всё зелёное. Дефект в оснастке проверки, не в
приложении; передан APP хэндоффом 28.08. Релиз им не задержан.

**Мерж потребовал ручного разбора двух конфликтов** — `BOARD.md` (обе роли переписали свои
строки) и `ACTIVITY.md` (обе дописали в конец). В журнале сохранены обе стороны по хронологии,
ничего не выброшено; строка APP в `BOARD.md` взята новая, строка ORCH обновлена по факту.

`runs/` закрыт `.gitignore`: `smoke_cwapp` пишет туда 15 PNG живого окна при каждом прогоне, и
каталог оставался неотслеженным — один невнимательный `git add` увёл бы снимки в репозиторий.

**Заключение о пуше — положительное**, обоснование в ответе владельцу: ядро не менялось, данных
экземпляра прибора и секретов в диффе нет, паспорта закрыты, новый сетевой запрос (проверка
обновлений) идёт только по нажатию, на публичный `api.github.com`, и ничего о приборе не
отправляет.

## [2026-08-28] [ORCH] Выпуск 0.1.0: лицензии, CHANGELOG, тег, сборка, архив с SHA-256

Порядок §6 пройден целиком, четыре решения владельца от 28.08 исполнены.

**Полные тексты лицензий (шаг 5).** `LICENSES.txt` вырос со ссылок до 190 КБ: LGPL-3.0 и GPL-3.0
взяты с `www.gnu.org` (канонический источник, 7 652 и 35 149 Б, целостность проверена по началу и
концу), MIT pyqtgraph, NumPy, Matplotlib и PSF — из метаданных установленных пакетов тех же
версий, что идут в сборку. В колесе PySide6 полных текстов нет вовсе, только ссылка на
коммерческую лицензию Qt — отсюда и загрузка. Добавлены точные версии состава и раздел «как
заменить библиотеку Qt»: ради этого права поставка и перестала быть onefile, и получателю надо
объяснить, где лежат `.dll` и что менять.

**CHANGELOG (шаг 2).** Раздел `[0.1.0]` по разделам «изменено / добавлено / исправлено».
Изменение опоры нормировки помечено явно как меняющее выдаваемые числа при ψ ≠ 0. Историю
переноса убрал под заголовок «Предыстория 0.1.0», «Открытые задачи» подняты на верхний уровень —
на них ссылается `CLAUDE.md`.

**Тег (шаг 4).** `v0.1.0`, аннотированный, на `b673fd9`. Версия в `cwapp/__init__.py` была
`0.1.0` — поднимать нечего.

**Сборка и архив.** PyInstaller 6.22.2, `--clean`, каталогом:

| Что | Значение |
|---|---|
| каталог | `build/dist/THz-Attenuator/`, 144 МБ |
| рядом с `.exe` | `LICENSES.txt` 195 091 Б, `_internal/` с Qt-DLL отдельными файлами |
| архив | `THz-Attenuator-0.1.0-win64.zip`, 57.4 МБ |
| SHA-256 | `79ce44e1a6c9af8a3d6abf1ac7182e71b6a4ca90f4d8ece24927b1281196f9da` |

Сумма записана рядом файлом `.sha256` и проверена обратной сверкой (`sha256sum -c` → OK).

**Живой запуск собранного `.exe` — не импорт.** Окно поднялось за 12 с, 161 МБ памяти, заголовок
`THz Attenuator — CW`. На снимке: обе кривые, слайдеры углов, стрелки спинбоксов различимы,
опора названа явно («Normalised to maximum at θ₁ = θ₂ = 0»), строка состояния —
`built-in SAMPLE — not your device · v0.1.0`, то есть паспорт рядом с программой не найден и взят
образец, как и должно быть в пустом каталоге. Ловушка `_MEIPASS` не сработала: поиск идёт от
`sys.executable`.

`build/work/` закрыт `.gitignore` — PyInstaller пишет туда временные объекты.

**За владельцем:** публикация GitHub Release (у меня нет `gh`) — приложить архив и сумму, текст
описания готов в разделе `[0.1.0]` CHANGELOG. После публикации стоит нажать в окне «Check for
updates»: сейчас кнопка честно отвечает «релизов пока нет», после — должна сказать, что
установлена последняя.

## [2026-08-28] [ORCH] Выпуск 0.1.0: лицензии, CHANGELOG, тег, сборка, архив с SHA-256

Порядок §6 пройден целиком.

**Полные тексты лицензий (шаг 5).** `LICENSES.txt` вырос со ссылок до 190 КБ: LGPL-3.0 и GPL-3.0
взяты с `www.gnu.org` (7 652 и 35 149 Б, целостность проверена по началу и концу), MIT pyqtgraph,
NumPy, Matplotlib и PSF — из метаданных установленных пакетов тех же версий, что идут в сборку.
В колесе PySide6 полных текстов нет вовсе, только ссылка на коммерческую лицензию Qt — отсюда и
загрузка. Добавлены точные версии состава и раздел «как заменить библиотеку Qt»: ради этого права
поставка и перестала быть onefile, и получателю надо сказать, где лежат `.dll` и что менять.

**CHANGELOG (шаг 2).** Раздел `[0.1.0]` по разделам «изменено / добавлено / исправлено».
Изменение опоры нормировки помечено явно как меняющее выдаваемые числа при ψ ≠ 0. История
переноса убрана под заголовок «Предыстория 0.1.0», «Открытые задачи» подняты на верхний уровень —
на них ссылается `CLAUDE.md`.

**Тег (шаг 4).** `v0.1.0`, аннотированный, на `b673fd9`. Версия в `cwapp/__init__.py` была
`0.1.0` — поднимать нечего.

**Сборка и архив.** PyInstaller 6.22.2, `--clean`, каталогом:

| Что | Значение |
|---|---|
| каталог | `build/dist/THz-Attenuator/`, 144 МБ |
| рядом с `.exe` | `LICENSES.txt` 195 091 Б, `_internal/` с Qt-DLL отдельными файлами |
| архив | `THz-Attenuator-0.1.0-win64.zip`, 57.4 МБ |
| SHA-256 | `79ce44e1a6c9af8a3d6abf1ac7182e71b6a4ca90f4d8ece24927b1281196f9da` |

Сумма записана рядом файлом `.sha256` и проверена обратной сверкой (`sha256sum -c` → OK).

**Живой запуск собранного `.exe`, не импорт.** Окно поднялось за 12 с, 161 МБ памяти, заголовок
`THz Attenuator — CW`. На снимке: обе кривые, слайдеры углов, стрелки спинбоксов различимы, опора
названа явно («Normalised to maximum at θ₁ = θ₂ = 0»), строка состояния —
`built-in SAMPLE — not your device · v0.1.0`, то есть паспорт рядом с программой не найден и взят
образец, как и должно быть в пустом каталоге. Ловушка `_MEIPASS` не сработала: поиск идёт от
`sys.executable`.

`build/work/` закрыт `.gitignore` — PyInstaller пишет туда временные объекты.

**За владельцем:** публикация GitHub Release (у ORCH нет `gh`) — приложить архив и сумму, текст
описания готов в разделе `[0.1.0]` CHANGELOG. После публикации стоит нажать в окне «Check for
updates»: сейчас кнопка честно отвечает «релизов пока нет», после — должна сказать, что
установлена последняя.

