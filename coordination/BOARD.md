# BOARD — сводка по ролям (одна строка на роль)

> Экран для владельца и ORCH: где что стоит и чего ждёт. Подробности роли — в
> `coordination/roles/<ID>.md`, историю пишет `ACTIVITY.md`. Кто сейчас работает — `claude agents`,
> не этот файл.

| Роль | Где стоит | Чего ждёт | Файл |
|---|---|---|---|
| **APP** приложение | правки 1, 2, 4 приняты ORCH (приёмка прогнана самостоятельно: `cwapp` **17/17**, база 13/13, сервисное OK, CW-кривая 5/5, ядро 9 файлов, опорная точка CLI −4.77 дБ сходится) и влиты в основную линию 27.08 | правки 3 — нормировка **на максимум** (П-2 решён), вместе с пересмотром M2; и `gui.spec` на `COLLECT` по П-1 (`HANDOFFS.md` 27.08) | `roles/APP.md` |
| **ORCH** координация и релизы | П-1 и П-3 закрыты решениями владельца 27.08 (поставка **не onefile**; тег отложен до правки 3), работа APP принята и влита, ревизия скилла сделана — апстрим по-прежнему `2e37500`, обновлять нечего | правки 3 от APP: после неё CHANGELOG → тег `v0.1.0` → сборка папкой → zip + SHA-256. публикации репорта по дашборду в апстрим (разрешена, но `gh` не установлен — черновик готов) | `roles/ORCH.md` |

## Что держит проект прямо сейчас

1. **Правка 3 из четырёх** — единственное, что стоит перед тегом `v0.1.0`. П-2 закрыт 27.08:
   нормировать **на максимум**; тянет за собой пересмотр проверки M2 приёмки. Три остальные
   приняты и влиты.
2. ~~LGPL и onefile~~ — **закрыто 27.08** (П-1): поставка не onefile, `COLLECT` и zip с SHA-256.
   Осталось исполнение в `gui.spec` — задание у APP.
3. **Дашборд слежения — не использовать на запись.** Врёт по русским журналам молча и коммитит в
   один клик; `append_question` портит таблицу чисел по П-2. Разбор — `HANDOFFS.md` 27.08,
   репорт в апстрим ждёт разрешения на публикацию.
4. ~~PR в апстрим скилла~~ — **закрыто 27.08**: все три дефекта хука исправлены в апстриме,
   локальных правок нет; ревизия 27.08 подтвердила, что апстрим стоит на том же `2e37500`.

> **Бюджет ролей — мягкий; выходить за него можно, если это обосновано** (решение владельца
> 27.08). Хук предупреждает, но не блокирует. Обоснование — содержательное: живой пункт, число,
> ловушка, из-за которой роль иначе потеряет заход. Не обоснование — «лень выносить в журнал»:
> закрытая история и подробности задач уходят в `ACTIVITY.md` и `HANDOFFS.md` по смыслу, а не по
> счётчику байтов. Сейчас `APP.md` 2445 Б и `ORCH.md` 2982 Б при лимите 2400 — превышение
> осознанное: у ORCH это три живых пункта (релизный порядок, форма поставки по П-1, запрет на
> запись через дашборд), каждый из которых иначе стоит роли одного захода.
