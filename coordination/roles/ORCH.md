# Роль ORCH — координация и релизы

**Имя сессии:** `orch` · **префикс коммита:** `[ORCH]`

## Пишу
`coordination/**` · `CLAUDE.md` · `README.md` · `CHANGELOG.md` · `LICENSES.txt` · `.gitignore` ·
`.claude/**` · git-мержи и теги

## Не пишу
Код приложения и физику. Только координация, релизы и поставка.

## Что открыто и что дальше

1. **Выпуск релиза** — порядок в `13_CW_APP_RELEASES.md`, раздел 6. Тег `v0.1.0` **не
   поставлен**: по П-3 ждёт правки 3, иначе релиз устареет по числам в день выпуска. Дальше —
   CHANGELOG, тег, сборка, zip, **SHA-256 на архив**, `LICENSES.txt` в комплекте (полные тексты
   лицензий, а не ссылки).
2. **П-1 закрыт 27.08: поставка не onefile** — `COLLECT`, Qt-DLL отдельными файлами рядом с
   `.exe`. Исполнение в `build/gui.spec` передано APP (их зона). Перед релизом сверить, какие
   модули Qt реально попали в сборку.
3. **Дашборд слежения — не использовать на запись** (разбор APP 27.08): врёт по русским
   журналам молча и коммитит в один клик, `append_question` портит таблицу чисел по П-2.
   Отправлено в апстрим как issue #6 (27.08); текст — `coordination/upstream/issue-dashboard.md`.
4. **Скилл на чистом апстриме `2e37500`.** Три дефекта хука закрыты в апстриме по репорту
   (issue #4), локальных правок нет. Осталось нерешённым в апстриме: 22 `.pyc` и `.coverage` в
   репозитории ломают клон на Windows без `core.longpaths` — при обновлении ставить через
   `tar --exclude`, сюда они не попадают.

## Гардрейлы, за которыми слежу
Репозиторий **публичный**: ни серийников, ни реальных калибровок, ни измеренных спектров.
`git push` — только с разрешения владельца. Физика — снимок; расхождение ловит
`tools/verify_core.py`.

## Решения владельца
`QUESTIONS.md`. Ключевые: продукт `cwapp`, Win7 снят (В-36) · публичный, Apache 2.0 (В-37,
В-39) · паспорта обезличенным образцом (В-40) · `track_viewer` не едет (В-42) · tkinter-линия
перенесена целиком вопреки §4 плана релизов — раздел устарел.
