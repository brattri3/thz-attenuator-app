# HANDOFFS — cross-role requests (work outside your own zone)

If the owner or you hit a task outside your specialty — don't do it yourself (`CHARTER.md §3`).
Write the request here, offer to switch the owner into the right session or launch it
(`LAUNCH_PROMPTS.md`).

**Format is not optional — a tool depends on it.** Each entry:

```
## [ISO-date] FROM <ID> TO <ID> — title
> Формат записи — как у живых записей ниже: дата, ОТ кого К кому, что нужно,
> условия и ловушки, статус. Шаблонные примеры удалены при развёртывании:
> дашборд читает этот файл, и выдуманные записи в нём выглядят как реальные.

## [2026-08-27] FROM ORCH TO toolsmith — Create requirements.txt for dashboard
- What: Create ssets/coordination/tools/dashboard/requirements.txt containing dependencies for the new Streamlit dashboard (streamlit, pytest, etc.). Also update 
eferences/setup.md to mention running pip install -r assets/coordination/tools/dashboard/requirements.txt.
- Context: We just built the dashboard but haven't formally documented its Python dependencies in a standard format.
- Done when: requirements.txt exists and setup.md is updated.
- **Status:** done

---

## [2026-08-27] ОТ ВЛАДЕЛЬЦА К APP — четыре правки интерфейса `cwapp`

Переданы владельцем 27.08 по итогу работы с приложением руками. Порядок — как ниже.

### 1. Кнопки вверх не работают, только вниз

`cwapp/widgets/params.py:33` — `QDoubleSpinBox` с `setRange(lo, hi)` и `setSingleStep(step)`.
Начинать с `setRange`: похоже на верхнюю границу, упирающуюся в текущее значение.

Дословно владелец: «НЕ работают кнопки вверх только кнопки вниз. Вообще кнопками не очень
удобно задаваться» — отсюда же пункт 2.

### 2. Слайдеры к полям углов ротаторов

Слайдеры, дублирующие поля ввода и синхронные с ними в обе стороны, шаг 1° — как у поля.
**Только полям углов ротаторов**; остальным достаточно ввода с клавиатуры.

### 3. Нормировка с учётом азимута поляризации источника

`cwapp/model.py`. Если правка сводится к тому, чтобы опора считалась при фактическом ψ, а не при
ψ=0 — делать. **Если упрётесь в выбор «нормировать на максимум или на ось прибора» — остановиться
и спросить владельца:** при повёрнутом источнике эти варианты расходятся содержательно, потому
что скрещенная пара одинаковых WGP поляризационно нейтральна и у дна воспроизводит состояние
источника, а не ось второй решётки. Это вопрос физической конвенции, а не техники.

### 4. Паспорт прибора — отдельным файлом рядом с программой

`cwapp/state.py`. Порядок поиска: явный выбор в интерфейсе → каталог рядом с исполняемым файлом
→ зашитый `SAMPLE` как последний рубеж; в интерфейсе видно, какой файл в итоге взят.

⚠ **Ловушка PyInstaller, из-за которой это ломается молча.** «Рядом с `.exe`» — это
`Path(sys.executable).parent`, а **не** `Path(__file__).parent`: под PyInstaller `__file__`
указывает во временный распакованный `_MEIPASS`. Из исходников ошибка не проявляется вовсе, а в
собранном `.exe` даёт подхват зашитого образца вместо паспорта, положенного оператором — то есть
неверные числа при внешне исправной работе.

- **Статус записи:** open

---

## [2026-08-27] ОТ APP К ORCH — обновить ожидаемые числа приёмки в `CLAUDE.md`

`CLAUDE.md` (зона ORCH) обещает `python -m attenuator_app.cwapp.selftest # 12/12`. После правок
1, 2 и 4 из хэндоффа владельца там **17/17**: добавлены M13…M17 -- порядок поиска паспорта,
поддельный `sys.frozen` против ловушки PyInstaller, громкий отказ явно выбранного негодного
файла, имя файла в строке состояния, образец последним рубежом. Остальные три прогона без
изменений (13/13, сервисное OK, 5/5), сверка ядра -- 9 файлов без расхождений.

Заодно стоит упомянуть в списке команд дымовой прогон: он вырос до 70 проверок и 15 снимков и
теперь закрывает стрелки спинбокса, слайдеры углов и выбор паспорта.

Правки лежат в ветке `role/app` тремя коммитами, **не запушены** -- `git push` ждёт прямого
разрешения владельца.

- **Статус записи:** open

---

## [2026-08-27] ОТ APP К ORCH — дашборд слежения врёт по русским журналам и небезопасен на запись

Дашборд (`coordination/tools/dashboard/**`) поднят на `localhost:8502` и осмотрен целиком в режиме
только чтения: ни одна форма не отправлена, файлы и git не тронуты. Он в зоне ORCH и идёт чистым
апстримом `2e37500`, поэтому чинить его в этом репозитории означало бы форк -- передаю как есть.

### 1. Экран не сходится с журналами, и об этом нигде не сказано

Причина общая: парсер написан под английские журналы, а журналы здесь русские. Ошибок при этом
не показывается -- оператор видит числа и верит им.

| Что на экране | Что в файлах | Откуда |
|---|---|---|
| `Open Questions: 0` | П-1 и П-3 открыты, рядом же их карточки помечены «🔴 OPEN» | `parser.py:150` -- `"open" in status`, «открыт»/«решён» не подходят |
| П-1 «ℹ️ Non-blocking» | «блокирующий первый внешний релиз» | `parser.py:151` -- `"blocking" in type` |
| `Roles Active: 0 / 2`, обе «idle/stale» | обе роли живые | следствие тех же словарей |
| Статусы ролей «🔹 правки», «🔹 переезд»; имена «APP** приложение» | колонка «Где стоит» со всей приёмкой | `parser.py:94` ждёт схему `Role / Status (date) / Summary`, у нас `Роль / Где стоит / Чего ждёт / Файл`; `parser.py:84` не снимает внутренние `**` |
| Обе записи 27.08 -- «⚠️ MISSING», `FROM ➔ TO` пустое, поля пустые | самые содержательные блоки файла | `parser.py:12-13` знает только `**Status:**` и `FROM x TO y`; поля читаются из буллетов `- What:`, у нас заголовки `### 1. …` |

`ACTIVITY.md` и `roles/*.md` дашборд не читает вовсе -- ссылок на них в коде нет, хотя `BOARD.md`
отсылает читателя именно туда.

### 2. Запись опасна -- это важнее косметики

- **`append_question` вставляет новую строку после последней табличной строки во всём файле**
  (`mutator.py:217`). Последняя таблица в нынешнем `QUESTIONS.md` -- физические числа по П-2
  (ψ / A(0,0) / максимум): новый вопрос уедет внутрь неё и испортит.
- **«Register New Role» дописывает строку в конец `BOARD.md`** (`dashboard.py:333`) -- под
  блоквот про бюджет ролей, вне таблицы, и с тремя колонками вместо четырёх.
- **Ни одного подтверждения у разрушительных действий:** «Update Role & Commit», «Submit Question
  & Commit», «Mark Done», «Reopen», «Rebuild INDEX.md», «Create Worktree» -- один клик пишет в файл
  и сразу коммитит. Ни диалога, ни превью диффа, ни отмены.
- **Выключатель Git Auto-Commit недоступен:** он в боковой панели, а она в этой сборке Streamlit
  (1.62.0) не рендерится вовсе -- проверено отдельным минимальным приложением, то есть виновата
  версия, а не код. Значение по умолчанию -- «включено».
- Нечёткое сопоставление колонок (`mutator.py:77`): пустой заголовок совпадает с любым запросом.
  Для нынешних русских шапок совпадений нет вовсе -- формы Update Role / Resolve просто вернут
  «not found», молча.
- `GitService.create_worktree` (`git_service.py:226`) жёстко зашивает `assets/.worktrees/<роль>`,
  а проект использует `.worktrees/`; подсказка «cd assets/.worktrees/…» тоже неверна.

Сам `git commit --only <path>` с ретраями по `index.lock` сделан аккуратно -- изоляция коммита в
порядке, претензия только к тому, ЧТО он коммитит.

### 3. Вёрстка

KPI-строка -- ради неё дашборд и открывают -- монохромная: все дельты серым `rgba(49,51,63,0.6)`
на белом (≈4.0:1 при 14 px, ниже порога WCAG AA 4.5:1), а красная подсветка «N blocking» не
включается никогда из-за той же ошибки типа. Статусы закодированы **только эмодзи** -- для
дальтоников и в ч/б различий нет. Текст вопроса набран заголовочным кеглем 24 px, карточка роли
(28 px) спорит с заголовком раздела. Сетка Roles Board жёстко в три колонки при двух ролях. На
375 px полоса вкладок скроллится горизонтально без видимого признака прокрутки -- «Git Worktrees»
и «Backlog Index» уезжают за край.

### 4. Что предлагается

Порядок починки по важности: погасить мутирующие формы до появления подтверждения и превью диффа
(или поднять переключатель коммита из боковой панели в основную область, дефолт `False`); русские
словари статусов и типов с нормализацией «е/ё», сведённые в один источник с `components.py`; схема
`BOARD.md` -- либо парсер по фактическим заголовкам, либо явный баннер «схема не распознана»
вместо подстановки первого слова прозы; `HEADER_RE`/`STATUS_RE` под «ОТ … К …» и «Статус записи»;
`append_question` -- вставка в таблицу по её заголовку; косметика последней.

Решение о репорте в апстрим -- за ORCH и владельцем: прошлый раз три дефекта хука ушли в апстрим
issue #4 и были исправлены там, форка не заводили. Полный разбор с привязками -- эта запись;
дашборд остаётся запущенным на 8502, до решения им лучше **не пользоваться на запись**.

- **Статус записи:** open

---

## [2026-08-27] ОТ ORCH К APP — `build/gui.spec` перевести с onefile на `COLLECT`

Решение владельца по П-1 (27.08): поставка **не onefile**. PySide6 идёт под LGPL, и у получателя
должна оставаться возможность заменить Qt своей сборкой; одиночный `.exe` этого не даёт, папка с
отдельными Qt-DLL — даёт. `build/gui.spec` в зоне APP, ORCH его не правит.

Что сделать:

- заменить одиночный `EXE(...)` на пару `EXE(...) + COLLECT(...)`, `console=False` оставить;
- проверить, что приложение запускается из собранного каталога, а не только из исходников:
  под PyInstaller `__file__` смотрит во временный `_MEIPASS`, поиск паспорта уже опирается на
  `Path(sys.executable).parent` (правка 4) — на собранной папке это надо увидеть глазами;
- рядом с `.exe` должен лежать `LICENSES.txt` (план релизов §6 п.5);
- сказать ORCH, какие модули Qt реально попали в сборку: перед первым внешним релизом надо
  сверить, не подтянул ли PyInstaller модулей с иными условиями (пункт 4 в `LICENSES.txt`).

Упаковку в zip, SHA-256 и публикацию делает ORCH — это не часть задания.

- **Статус записи:** open

---

## [2026-08-27] ОТ ORCH К APP — приёмка правок 1, 2, 4 принята, ветка влита

Прогнал приёмку сам, на ветке `role/app`, не со слов: `cwapp` **17/17**, база 13/13, сервисное
(модель/пределы/калибровка/приложение) OK, CW-кривая 5/5, сверка ядра 9 файлов без расхождений,
опорная точка CLI (40°/0°/0.8 ТГц) — −4.77 дБ, азимут +0.023°, эллиптичность +0.630°, совпадает с
записанным в `CLAUDE.md`. Числа в `CLAUDE.md` обновлены на 17/17, дымовой прогон добавлен в список
команд. Ветка влита в основную линию мержем `--no-ff`; `git push` по-прежнему не разрешён.

Правка 3 (нормировка на максимум, П-2 решён 27.08) — за APP, вместе с пересмотром проверки M2.
Тег `v0.1.0` ждёт её: релиз до правки 3 устарел бы по числам в тот же день (П-3).

- **Статус записи:** open

---

## [2026-08-28] ОТ APP К ORCH — всё к релизу `v0.1.0` готово, за вами CHANGELOG, тег и публикация

Владелец 28.08 выбрал сценарий «весь путь до релиза» и номер **`v0.1.0` без поднятия версии**:
в `cwapp/__init__.py` уже `0.1.0`, релизов не выходило, ломать нечего. Правку 3 проводит ORCH --
пушит и вливает он же (решение владельца там же).

### Что сделано APP (три коммита в `role/app`)

1. **Правка 3** -- опора нормировки стала максимумом по обоим углам (П-2). При ψ = 0 максимум
   лежит в нуле шкал, поэтому **ни одно прежнее число не сдвинулось**, опорная точка CLI прежняя;
   меняется только поведение при наклонённом источнике. Окно называет место опоры и предупреждает,
   что показания несравнимы с прогонами при другом азимуте источника.
2. **Поставка папкой** по П-1: `EXE + COLLECT`, Qt-DLL отдельными файлами, `LICENSES.txt` рядом с
   `.exe`. Ловушка PyInstaller проверена вживую на собранной папке.
3. **Проверка обновлений** вместо заглушки: `QtNetwork`, без новых зависимостей, окно не замирает.

### Приёмка

`cwapp` **27/27** (было 21/21), база 13/13, сервисное OK (калибровка 8/8, приложение 7/7),
CW-кривая 5/5, сверка ядра 9 файлов, дымовой прогон зелёный с живым сетевым запросом. Опорная
точка CLI (40°/0°/0.8 ТГц) -- −4.77 дБ, азимут +0.023°, эллиптичность +0.630°.

### Состав модулей Qt, как просили

Модули: `QtCore`, `QtGui`, `QtWidgets`, `QtNetwork`, `QtOpenGL`, `QtOpenGLWidgets`, `QtSvg`,
`QtTest`. Плагины: `generic`, `iconengines`, `imageformats`, `networkinformation`, `platforms`,
`styles`, `tls`. Всё — из PySide6-Essentials под LGPL-3.0; ничего за пределами Essentials
PyInstaller не подтянул, QtWebEngine и Charts в сборке нет. `QtOpenGL`, `QtSvg` и `QtTest` тянет
pyqtgraph. Размер каталога 144 МБ (было 215 -- исключён scipy, который тянулся транзитивно и не
используется). Рядом с `.exe`: `THz-Attenuator.exe`, `_internal/`, `LICENSES.txt`.

### Что за ORCH

1. **CHANGELOG** (§6 п.2) -- файл в зоне ORCH. Готовый текст блока, с обязательной пометкой того,
   что меняет числа:

   > **Изменено.** Опора нормировки CW-приложения -- максимум пропускания по обоим углам, а не
   > отсчёт при θ₁ = θ₂ = 0 (решение владельца П-2). ⚠ **Меняет выдаваемые числа при повёрнутом
   > источнике** (ψ ≠ 0): при ψ = 45° опора уезжает на θ₁ +30°, θ₂ +15°. При ψ = 0 числа не
   > изменились. Показания несравнимы между прогонами с разным азимутом источника -- окно об этом
   > предупреждает.
   > **Добавлено.** Слайдеры к углам ротаторов, шаг 1°. Поиск паспорта рядом с программой с
   > показом взятого файла. Проверка обновлений по GitHub Releases, без автообновления.
   > Поставка каталогом вместо onefile (П-1).
   > **Исправлено.** Стрелки спинбоксов рисует нативный стиль -- кнопка «вверх» снова различима.

2. **Тег `v0.1.0`**, сборка, zip каталога `build/dist/THz-Attenuator/`, SHA-256 **на архив** (П-1),
   публикация с `LICENSES.txt`.
3. ⚠ **К первому внешнему релизу в `LICENSES.txt` нужны полные тексты лицензий, а не ссылки** --
   для LGPL-3.0 это обязательное условие (ваша же запись по П-1). Файл в зоне ORCH, я его не
   трогал; сейчас он едет в поставку как есть.
4. После тега стоит **перепроверить кнопку обновлений**: сейчас она честно отвечает «релизов пока
   нет» (GitHub отдаёт 404 до первого тега), а после публикации должна отвечать «установлена
   последняя». Проверяется одним нажатием.

- **Статус записи:** open

---

## [2026-08-28] ОТ ORCH К APP — дымовой прогон на платформе по умолчанию даёт 8 ложных отказов

Приёмка вашей работы прошла (числа ниже), но одно расхождение с передачей: у вас «дымовой прогон
зелёный», у меня при запуске **как написано в документации** — 8 отказов:

```
ОТКАЗОВ: 8
 - theta1: стрелка вверх различима   0 px при кнопке 14 px
 - theta1: стрелка вниз различима    4 px при кнопке 14 px
 ... то же для theta2, freq, psi
```

Причина не в приложении. `smoke_cwapp` ставит `QT_QPA_PLATFORM=offscreen` по умолчанию
(`smoke_cwapp.py:33`), а стрелки спинбокса рисует **нативный стиль** — под offscreen он их не
рисует вовсе. С `QT_QPA_PLATFORM=windows` тот же прогон зелёный целиком, проверено на
объединённом состоянии. Правка 1 в порядке: соседняя проверка «таблица стилей не забирает
отрисовку спинбокса» проходит на обеих платформах.

Дефект в самой проверке, и он злее, чем кажется: 0 px и 4 px — ровно те числа, которыми
`smoke_cwapp.py:296` описывает **настоящий** дефект («вырождается в 4 px из 20»). То есть прогон
по умолчанию показывает картину уже исправленной поломки. Следующий, кто это увидит, либо
поверит и пойдёт чинить исправное, либо привыкнет к красному — и пропустит настоящий возврат.

Как чинить — за вами (файл в вашей зоне). Разумное: пропускать пиксельные проверки нативной
отрисовки под offscreen с явным «пропущено: платформа offscreen не рисует нативные стрелки», а не
молча; либо поднимать `windows` для этой группы проверок.

Тег `v0.1.0` этим не задержан: приложение исправно, отказ в оснастке проверки.

- **Статус записи:** open

